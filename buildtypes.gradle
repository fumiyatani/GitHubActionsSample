android {
    buildTypes {
        debug {
            println("BuildType is debug")
        }
        pre {
            println("BuildType is pre")
        }
        release {
            println("BuildType is release")
        }
    }
}